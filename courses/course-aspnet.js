window.COURSES = window.COURSES || {};
window.COURSES["aspnet"] = {
  "id": "aspnet",
  "title": "ASP.NET Core",
  "icon": "API",
  "summary": "Apprends à construire une Web API moderne avec ASP.NET Core : du fichier Program.cs et du serveur Kestrel jusqu'au pipeline de middlewares, l'injection de dépendances, les contrôleurs, le model binding, la validation, les codes HTTP REST et les bonnes pratiques (DTO, async, configuration). Un parcours débutant vers junior, illustré d'exemples C# concrets.",
  "chapters": [
    {
      "id": "intro-program-kestrel",
      "title": "1. Qu'est-ce qu'ASP.NET Core, Program.cs et Kestrel",
      "markdown": "## La métaphore : un restaurant\n\nImagine un **restaurant**. Le client (le navigateur ou une app mobile) passe une commande (une requête HTTP). En cuisine, il y a une organisation : un serveur qui accueille (Kestrel), une chaîne de postes par lesquels la commande passe (le pipeline), et des cuisiniers spécialisés qui préparent le plat (tes contrôleurs et services). À la fin, le plat (la réponse HTTP, souvent du JSON) revient au client.\n\n**ASP.NET Core**, c'est tout ce restaurant fourni clé en main par Microsoft : un framework open-source et multiplateforme (Windows, Linux, macOS) pour construire des applications web et des **Web API** en C#.\n\n## Définition\n\nASP.NET Core est le framework web de la plateforme .NET. Il sert à créer :\n\n- des **Web API** (services HTTP qui renvoient du JSON, consommés par un front Angular/React ou une app mobile) ;\n- des sites web rendus côté serveur (MVC, Razor Pages, Blazor).\n\nDans ce cours, on se concentre sur la **Web API**, le cas le plus courant pour un dev backend junior.\n\n### Pourquoi « Core » ?\n\nL'ancien ASP.NET (dit « .NET Framework ») ne tournait que sur Windows et était lié à IIS. **ASP.NET Core** a été réécrit pour être :\n\n- **multiplateforme** (Linux, conteneurs Docker, cloud) ;\n- **performant** (un des frameworks web les plus rapides) ;\n- **modulaire** : tu n'embarques que ce dont tu as besoin.\n\n## Kestrel : le serveur web intégré\n\n**Kestrel** est le serveur HTTP fourni avec ASP.NET Core. C'est lui qui écoute sur un port (ex. `https://localhost:5001`), reçoit les requêtes brutes du réseau et les transmet à ton application.\n\nKestrel est rapide et fonctionne seul, mais en production on le place souvent **derrière un reverse proxy** (Nginx, IIS, Apache) qui gère le TLS, la compression, l'équilibrage de charge, etc. Ce proxy s'appelle un *reverse proxy* ; Kestrel reste le moteur applicatif.\n\n```\nClient  ->  (Nginx / IIS)  ->  Kestrel  ->  Ton application ASP.NET Core\n```\n\n## Program.cs : le point d'entrée\n\nDepuis .NET 6, tout démarre dans un seul fichier : **`Program.cs`**. Il utilise les *top-level statements* (pas de classe `Program` ni de `Main` visibles). Il se lit en deux temps :\n\n1. **Configuration des services** (on remplit le `builder`) — qui sera disponible dans l'application.\n2. **Configuration du pipeline** (on configure l'`app`) — comment chaque requête est traitée.\n\n```csharp\nvar builder = WebApplication.CreateBuilder(args);\n\n// 1) Enregistrement des services (injection de dépendances)\nbuilder.Services.AddControllers();\nbuilder.Services.AddEndpointsApiExplorer();\nbuilder.Services.AddSwaggerGen();\n\nvar app = builder.Build();\n\n// 2) Configuration du pipeline HTTP (les middlewares)\nif (app.Environment.IsDevelopment())\n{\n    app.UseSwagger();\n    app.UseSwaggerUI();\n}\n\napp.UseHttpsRedirection();\napp.UseAuthorization();\napp.MapControllers();\n\napp.Run(); // démarre Kestrel et bloque le thread principal\n```\n\nPoints clés à retenir :\n\n- `WebApplication.CreateBuilder(args)` prépare la config, le logging, et le conteneur de services.\n- `builder.Services.Add...` : on **enregistre** les services (avant `Build()`).\n- `builder.Build()` produit l'objet `app`.\n- `app.Use...` / `app.Map...` : on **configure le pipeline** (après `Build()`).\n- `app.Run()` lance Kestrel et garde le programme actif.\n\n## L'environnement (Development / Production)\n\nASP.NET Core lit la variable d'environnement `ASPNETCORE_ENVIRONMENT`. Les valeurs habituelles sont `Development`, `Staging`, `Production`. Cela permet d'activer des comportements différents (ex. afficher Swagger ou des pages d'erreur détaillées **uniquement** en développement).\n\n## Test de Feynman\n\nDis-le à voix haute, comme à un ami : « ASP.NET Core est le framework .NET pour faire des API web. **Kestrel** est le serveur qui écoute le réseau. **Program.cs** est le point d'entrée : d'abord j'enregistre mes services sur le `builder`, j'appelle `Build()`, puis je configure le pipeline sur l'`app`, et enfin `app.Run()` démarre tout. » Si tu sais expliquer ça simplement, le chapitre est acquis.\n\n## À retenir\n\n| Élément | Rôle |\n|---|---|\n| ASP.NET Core | Framework web .NET, multiplateforme |\n| Kestrel | Serveur HTTP intégré qui écoute le réseau |\n| Reverse proxy | Nginx/IIS devant Kestrel en production |\n| Program.cs | Point d'entrée : services puis pipeline |\n| `builder.Services` | Enregistrement des services (avant Build) |\n| `app.Use... / app.Map...` | Configuration du pipeline (après Build) |",
      "playground": null,
      "quiz": [
        {
          "question": "Quel composant joue le rôle de serveur HTTP intégré dans ASP.NET Core ?",
          "options": ["IIS uniquement", "Kestrel", "Apache Tomcat", "Node.js"],
          "correctIndex": 1,
          "explanation": "Kestrel est le serveur web multiplateforme intégré à ASP.NET Core. IIS ou Nginx peuvent être placés devant lui comme reverse proxy, mais ce n'est pas obligatoire."
        },
        {
          "question": "Dans Program.cs, à quel moment enregistre-t-on les services (injection de dépendances) ?",
          "options": ["Sur builder.Services, AVANT builder.Build()", "Sur app, APRÈS builder.Build()", "Dans app.Run()", "Peu importe l'ordre"],
          "correctIndex": 0,
          "explanation": "On enregistre les services sur builder.Services avant l'appel à builder.Build(). Après Build(), on configure le pipeline de middlewares sur l'objet app."
        },
        {
          "question": "Que fait l'appel app.Run() à la fin de Program.cs ?",
          "options": ["Il compile le projet", "Il démarre le serveur Kestrel et maintient l'application en écoute", "Il exécute les tests unitaires", "Il enregistre les contrôleurs"],
          "correctIndex": 1,
          "explanation": "app.Run() démarre le serveur (Kestrel) et bloque le thread principal pour que l'application continue d'écouter les requêtes entrantes."
        }
      ]
    },
    {
      "id": "pipeline-middlewares",
      "title": "2. Le pipeline et les middlewares",
      "markdown": "## La métaphore : la chaîne de contrôle à l'aéroport\n\nÀ l'aéroport, avant d'embarquer, tu passes une **chaîne de postes** dans un ordre précis : enregistrement, contrôle des billets, sécurité, douane, porte d'embarquement. Chaque poste peut te **laisser passer** au suivant, ou **te bloquer** et te renvoyer (ex. la sécurité te refuse l'accès).\n\nDans ASP.NET Core, c'est pareil : chaque requête HTTP traverse un **pipeline** de **middlewares**, dans un ordre que TU définis. Chaque middleware peut traiter la requête, la modifier, la laisser continuer, ou court-circuiter et renvoyer une réponse immédiatement.\n\n## Définition : middleware et pipeline\n\nUn **middleware** est un composant qui reçoit la requête, fait son travail, puis (optionnellement) appelle le middleware suivant via `next()`. L'enchaînement de tous les middlewares forme le **pipeline**.\n\nLe pipeline est **bidirectionnel** : la requête descend la chaîne, atteint le bout (le contrôleur), puis la réponse remonte la chaîne en sens inverse.\n\n```\nRequête  ->  Logging  ->  HTTPS  ->  Routing  ->  Auth  ->  Endpoint (contrôleur)\nRéponse  <-  Logging  <-  HTTPS  <-  Routing  <-  Auth  <-  Endpoint\n```\n\n## L'ORDRE est crucial\n\nC'est l'erreur n°1 des débutants : **l'ordre des `app.Use...` détermine l'ordre d'exécution**. Un middleware placé trop tôt ou trop tard casse le comportement.\n\nL'ordre recommandé pour une Web API ressemble à ceci :\n\n```csharp\nvar app = builder.Build();\n\napp.UseExceptionHandler(\"/error\"); // gestion globale des erreurs (tout en haut)\napp.UseHttpsRedirection();          // force le HTTPS\napp.UseStaticFiles();               // sert les fichiers statiques (si besoin)\n\napp.UseRouting();                   // décide QUEL endpoint correspond à l'URL\n\napp.UseCors();                      // après Routing, avant Auth\napp.UseAuthentication();            // QUI es-tu ? (identité)\napp.UseAuthorization();             // AS-TU le droit ? (permissions)\n\napp.MapControllers();               // exécute l'endpoint choisi\n\napp.Run();\n```\n\n### Les trois middlewares clés\n\n- **`UseRouting()`** : analyse l'URL et détermine quel endpoint (quel contrôleur/action) correspond. Il met l'endpoint « de côté » mais ne l'exécute pas encore.\n- **`UseAuthentication()`** : répond à la question **« Qui es-tu ? »**. Il lit par exemple un token JWT et construit l'identité de l'utilisateur (`User.Identity`).\n- **`UseAuthorization()`** : répond à **« As-tu le droit ? »**. Il vérifie les permissions/rôles pour l'endpoint sélectionné.\n\nL'ordre logique est donc : **Routing → Authentication → Authorization → MapControllers**.\n\n> Pourquoi Authentication AVANT Authorization ? Parce qu'il faut d'abord savoir QUI fait la requête (authentification) avant de pouvoir vérifier S'IL A LE DROIT (autorisation). On ne peut pas vérifier des permissions pour un utilisateur inconnu.\n\n> Pourquoi après UseRouting ? Parce que l'autorisation peut dépendre de l'endpoint visé (certaines routes exigent un rôle précis). Il faut donc que le routing ait déjà choisi l'endpoint.\n\n## Écrire un middleware personnalisé\n\nLe plus simple : `app.Use` avec un délégué. Remarque l'appel à `await next()` qui passe la main au middleware suivant.\n\n```csharp\napp.Use(async (context, next) =>\n{\n    var start = DateTime.UtcNow;\n    await next(); // appelle la suite du pipeline\n    var ms = (DateTime.UtcNow - start).TotalMilliseconds;\n    Console.WriteLine($\"{context.Request.Path} a pris {ms} ms\");\n});\n```\n\nSi tu **n'appelles pas** `next()`, tu court-circuites le pipeline : la requête ne va pas plus loin et tu renvoies directement une réponse. C'est ce que fait `UseAuthorization` quand il bloque un accès non autorisé (réponse 401/403).\n\n### Use vs Map vs Run\n\n- **`Use`** : middleware qui peut appeler le suivant.\n- **`Run`** : middleware terminal (n'appelle jamais `next`), il finit le pipeline.\n- **`Map`** : branche le pipeline sur un préfixe d'URL.\n\n## Test de Feynman\n\nExplique-toi à toi-même : « Le pipeline est une chaîne de middlewares. Chaque requête la traverse dans l'ordre où je les ai déclarés. `UseRouting` choisit l'endpoint, `UseAuthentication` dit qui je suis, `UseAuthorization` dit si j'ai le droit, puis `MapControllers` exécute l'action. Si je mets `UseAuthorization` avant `UseAuthentication`, ça casse, parce qu'on vérifierait des droits avant de connaître l'identité. »\n\n## À retenir\n\n| Middleware | Question / rôle | Ordre |\n|---|---|---|\n| UseExceptionHandler | Capture les erreurs | Tout en haut |\n| UseHttpsRedirection | Force HTTPS | Tôt |\n| UseRouting | Quel endpoint ? | Avant Auth |\n| UseAuthentication | Qui es-tu ? | Après Routing |\n| UseAuthorization | As-tu le droit ? | Après Authentication |\n| MapControllers | Exécute l'action | À la fin |",
      "playground": null,
      "quiz": [
        {
          "question": "Quel est l'ordre correct de ces middlewares dans une Web API ASP.NET Core ?",
          "options": ["UseAuthorization, UseAuthentication, UseRouting", "UseRouting, UseAuthentication, UseAuthorization", "UseAuthentication, UseRouting, UseAuthorization", "UseAuthorization, UseRouting, UseAuthentication"],
          "correctIndex": 1,
          "explanation": "L'ordre est UseRouting (choisir l'endpoint), puis UseAuthentication (qui es-tu ?), puis UseAuthorization (as-tu le droit ?). On doit connaître l'identité avant de vérifier les permissions."
        },
        {
          "question": "Que se passe-t-il si un middleware n'appelle pas next() ?",
          "options": ["Le pipeline plante systématiquement", "La requête est court-circuitée : elle ne va pas plus loin et une réponse est renvoyée immédiatement", "Tous les middlewares suivants s'exécutent quand même", "Rien, next() est optionnel et sans effet"],
          "correctIndex": 1,
          "explanation": "Ne pas appeler next() court-circuite le pipeline : les middlewares suivants ne s'exécutent pas et la réponse est renvoyée directement. C'est ce que fait l'autorisation pour bloquer un accès (401/403)."
        },
        {
          "question": "Pourquoi UseAuthentication doit-il venir avant UseAuthorization ?",
          "options": ["Par convention de nommage alphabétique", "Parce qu'il faut d'abord établir l'identité de l'utilisateur avant de vérifier ses permissions", "Parce que l'autorisation est plus lente", "Aucune raison, l'ordre est indifférent"],
          "correctIndex": 1,
          "explanation": "L'authentification établit QUI fait la requête. L'autorisation vérifie ensuite si cet utilisateur identifié A LE DROIT d'accéder à la ressource. On ne peut pas vérifier les droits d'un utilisateur encore inconnu."
        }
      ]
    },
    {
      "id": "injection-dependances",
      "title": "3. L'injection de dépendances (DI)",
      "markdown": "## La métaphore : le branchement électrique\n\nChez toi, tes appareils ne contiennent pas leur propre centrale électrique : ils se **branchent** sur une prise, et le courant arrive de l'extérieur. Tu n'as pas à savoir d'où vient l'électricité, tu fais confiance à la prise.\n\nL'**injection de dépendances** (DI), c'est ce branchement : une classe **ne crée pas elle-même** les objets dont elle a besoin (ses dépendances) ; elle les **reçoit de l'extérieur**, via son constructeur. Le framework s'occupe de \"fournir le courant\".\n\n## Définition\n\nL'**injection de dépendances** est un patron de conception où un objet reçoit ses dépendances depuis l'extérieur au lieu de les instancier lui-même. ASP.NET Core fournit un **conteneur IoC** (Inversion of Control) intégré : tu **enregistres** tes services, et le framework les **injecte** automatiquement là où ils sont demandés.\n\n```csharp\n// Sans DI : la classe est couplée à l'implémentation concrète\npublic class CommandeService\n{\n    private readonly EmailSender _email = new EmailSender(); // rigide, difficile à tester\n}\n\n// Avec DI : la dépendance est injectée via le constructeur\npublic class CommandeService\n{\n    private readonly IEmailSender _email;\n    public CommandeService(IEmailSender email) // ASP.NET fournit l'instance\n    {\n        _email = email;\n    }\n}\n```\n\nAvantages : code **découplé**, **testable** (on injecte un faux service en test), et **interchangeable** (on change l'implémentation sans toucher aux classes clientes).\n\n## Les trois durées de vie (lifetimes)\n\nQuand tu enregistres un service, tu choisis sa **durée de vie**. C'est LE point d'entretien classique.\n\n```csharp\nbuilder.Services.AddTransient<IEmailSender, EmailSender>();\nbuilder.Services.AddScoped<ICommandeRepository, CommandeRepository>();\nbuilder.Services.AddSingleton<IClock, SystemClock>();\n```\n\n| Lifetime | Nouvelle instance... | Cas d'usage typique |\n|---|---|---|\n| **Transient** | à **chaque** demande/injection | services légers, sans état |\n| **Scoped** | **une par requête HTTP** | repositories, `DbContext` (EF Core) |\n| **Singleton** | **une seule** pour toute l'application | cache, configuration, horloge |\n\n- **Transient** : à chaque fois qu'on en a besoin, une nouvelle instance est créée. Même au sein d'une même requête, deux injections donnent deux objets différents.\n- **Scoped** : une instance par **requête HTTP**. Toutes les classes d'une même requête partagent la même instance. C'est le choix par défaut pour un `DbContext` Entity Framework.\n- **Singleton** : une seule instance pour toute la durée de vie de l'application, partagée par toutes les requêtes. **Attention au thread-safety** : plusieurs requêtes y accèdent en parallèle.\n\n## Le piège de la « captive dependency »\n\nC'est un piège classique posé en entretien. **Règle d'or : ne jamais injecter un service à durée de vie PLUS COURTE dans un service à durée de vie PLUS LONGUE.**\n\nExemple du problème : un **Singleton** qui dépend d'un **Scoped**.\n\n```csharp\nbuilder.Services.AddSingleton<MonCache>();          // vit pour toujours\nbuilder.Services.AddScoped<ICommandeRepository, CommandeRepository>(); // 1 par requête\n\npublic class MonCache // SINGLETON\n{\n    private readonly ICommandeRepository _repo; // SCOPED capturé !\n    public MonCache(ICommandeRepository repo)\n    {\n        _repo = repo; // PROBLÈME : ce repo est \"capturé\" à vie\n    }\n}\n```\n\n**Pourquoi c'est un bug ?** Le Singleton est créé **une seule fois** et garde sa référence vers le repository Scoped **pour toujours**. Or, ce repository (et son `DbContext`) était censé vivre le temps d'**une seule requête**. On se retrouve avec un `DbContext` partagé entre toutes les requêtes : fuites mémoire, données obsolètes, erreurs de concurrence. On dit que la dépendance Scoped est **« captive »** du Singleton.\n\nLa bonne hiérarchie d'injection :\n\n```\nSingleton  ->  ne doit dépendre que de Singleton\nScoped     ->  peut dépendre de Scoped et Singleton\nTransient  ->  peut dépendre de tout\n```\n\n**Solution** : si un Singleton a besoin d'un service Scoped ponctuellement, il injecte un `IServiceScopeFactory` et crée un *scope* à la demande :\n\n```csharp\npublic class MonCache\n{\n    private readonly IServiceScopeFactory _scopeFactory;\n    public MonCache(IServiceScopeFactory scopeFactory) => _scopeFactory = scopeFactory;\n\n    public void RafraichirCache()\n    {\n        using var scope = _scopeFactory.CreateScope();\n        var repo = scope.ServiceProvider.GetRequiredService<ICommandeRepository>();\n        // ... utiliser repo le temps de ce scope, puis il est libéré\n    }\n}\n```\n\nNote : par défaut, en environnement de développement, ASP.NET Core **détecte** ce genre d'erreur au démarrage et lève une exception (validation du scope), ce qui aide à attraper le bug tôt.\n\n## Test de Feynman\n\n« L'injection de dépendances, c'est recevoir mes objets de l'extérieur au lieu de les créer moi-même. J'enregistre mes services dans `Program.cs` avec une durée de vie : **Transient** (nouvelle instance à chaque fois), **Scoped** (une par requête), **Singleton** (une pour toute l'app). Le piège de la *captive dependency* : un Singleton qui capture un Scoped le garde à vie, alors qu'il aurait dû mourir avec la requête. »\n\n## À retenir\n\n| Méthode | Durée de vie | Exemple |\n|---|---|---|\n| AddTransient | À chaque injection | Petit service sans état |\n| AddScoped | Une par requête HTTP | DbContext, repository |\n| AddSingleton | Une pour toute l'app | Cache, config, horloge |\n\nRègle : ne jamais capturer un service à vie plus courte dans un service à vie plus longue.",
      "playground": null,
      "quiz": [
        {
          "question": "Quelle durée de vie crée une nouvelle instance pour chaque requête HTTP (idéale pour un DbContext EF Core) ?",
          "options": ["Transient", "Scoped", "Singleton", "Static"],
          "correctIndex": 1,
          "explanation": "Scoped crée une instance par requête HTTP, partagée par toutes les classes de cette requête. C'est le choix recommandé pour un DbContext Entity Framework Core."
        },
        {
          "question": "Qu'est-ce qu'une 'captive dependency' ?",
          "options": ["Un service qui n'est jamais enregistré dans le conteneur", "Un service à durée de vie courte (ex. Scoped) capturé par un service à durée de vie plus longue (ex. Singleton)", "Un service Transient injecté deux fois", "Une dépendance circulaire entre deux contrôleurs"],
          "correctIndex": 1,
          "explanation": "Une captive dependency survient quand un service à vie longue (Singleton) capture un service à vie courte (Scoped), le maintenant en vie au-delà de sa portée prévue, ce qui provoque fuites et données obsolètes."
        },
        {
          "question": "Un service Singleton peut sans risque dépendre directement de quel type de service ?",
          "options": ["Uniquement d'autres Singletons", "De n'importe quel Scoped", "De n'importe quel Transient à état", "De tous les services sans distinction"],
          "correctIndex": 0,
          "explanation": "Un Singleton ne doit dépendre que d'autres Singletons. Pour utiliser un Scoped, il doit créer un scope à la demande via IServiceScopeFactory afin de ne pas le capturer à vie."
        }
      ]
    },
    {
      "id": "controleurs-routing",
      "title": "4. Contrôleurs, routing et attributs",
      "markdown": "## La métaphore : le standard téléphonique\n\nUn **standard téléphonique** dirige chaque appel vers le bon service : « tapez 1 pour les ventes, 2 pour le support ». Le **routing** d'ASP.NET Core fait pareil : selon l'URL et la méthode HTTP de la requête, il dirige vers la bonne **action** d'un **contrôleur**.\n\n## Définition : contrôleur et action\n\nUn **contrôleur** est une classe qui regroupe des **actions** (des méthodes), chacune répondant à une requête HTTP. Pour une Web API, le contrôleur hérite de `ControllerBase` et porte l'attribut `[ApiController]`.\n\n```csharp\n[ApiController]\n[Route(\"api/[controller]\")] // -> api/commandes\npublic class CommandesController : ControllerBase\n{\n    private readonly ICommandeService _service;\n\n    public CommandesController(ICommandeService service) // DI\n    {\n        _service = service;\n    }\n\n    [HttpGet] // GET api/commandes\n    public IEnumerable<Commande> GetToutes() => _service.Lister();\n\n    [HttpGet(\"{id}\")] // GET api/commandes/42\n    public ActionResult<Commande> GetUne(int id)\n    {\n        var commande = _service.Trouver(id);\n        if (commande is null) return NotFound();\n        return Ok(commande);\n    }\n}\n```\n\n### À quoi sert `[ApiController]` ?\n\nCet attribut active des comportements pratiques propres aux API :\n\n- **validation automatique** du modèle (renvoie un 400 si invalide, sans `if`) ;\n- **inférence des sources de binding** (ex. les types complexes viennent du corps) ;\n- réponses d'erreur au format **ProblemDetails** (voir chapitre 5).\n\n## Le routing par attributs\n\nDans une Web API moderne, on utilise le **routing par attributs** : la route est déclarée directement sur le contrôleur et l'action.\n\n- `[Route(\"api/[controller]\")]` sur la classe : `[controller]` est remplacé par le nom du contrôleur sans le suffixe « Controller ». `CommandesController` → `api/commandes`.\n- `[HttpGet(\"{id}\")]` sur l'action : combine avec la route du contrôleur → `api/commandes/{id}`.\n\n### Les attributs de verbe HTTP\n\n| Attribut | Verbe | Usage REST |\n|---|---|---|\n| `[HttpGet]` | GET | Lire une ou plusieurs ressources |\n| `[HttpPost]` | POST | Créer une ressource |\n| `[HttpPut]` | PUT | Remplacer une ressource entière |\n| `[HttpPatch]` | PATCH | Modifier partiellement |\n| `[HttpDelete]` | DELETE | Supprimer une ressource |\n\n## D'où viennent les paramètres ? Les attributs `[From...]`\n\nPour chaque paramètre d'une action, ASP.NET Core doit savoir **où** lire la valeur. C'est le **model binding** (détaillé au chapitre 5), guidé par des attributs :\n\n| Attribut | Source de la donnée |\n|---|---|\n| `[FromRoute]` | un segment de l'URL (ex. `{id}`) |\n| `[FromQuery]` | la query string (ex. `?page=2`) |\n| `[FromBody]` | le corps de la requête (JSON) |\n| `[FromHeader]` | un en-tête HTTP |\n| `[FromForm]` | un formulaire (multipart, upload) |\n| `[FromServices]` | un service du conteneur DI |\n\n```csharp\n[HttpGet] // GET api/commandes?page=2&taille=20\npublic IActionResult Lister([FromQuery] int page = 1, [FromQuery] int taille = 10)\n{\n    return Ok(_service.Lister(page, taille));\n}\n\n[HttpPost] // POST api/commandes  (corps = JSON)\npublic IActionResult Creer([FromBody] CreerCommandeDto dto)\n{\n    var creee = _service.Creer(dto);\n    // 201 Created + en-tête Location vers la nouvelle ressource\n    return CreatedAtAction(nameof(GetUne), new { id = creee.Id }, creee);\n}\n\n[HttpDelete(\"{id}\")] // DELETE api/commandes/42\npublic IActionResult Supprimer([FromRoute] int id)\n{\n    _service.Supprimer(id);\n    return NoContent(); // 204\n}\n```\n\n> Avec `[ApiController]`, ces attributs sont souvent **inférés** : un type complexe est supposé venir du corps (`[FromBody]`), un type simple présent dans le template de route vient de la route, sinon de la query string. Les écrire reste utile pour la clarté.\n\n## Parallèle Spring Boot (pour les profils Java)\n\nSi tu connais Spring Boot, la correspondance est directe :\n\n| ASP.NET Core | Spring Boot |\n|---|---|\n| `[ApiController]` + `ControllerBase` | `@RestController` |\n| `[Route(\"api/...\")]` | `@RequestMapping(\"/api/...\")` |\n| `[HttpGet(\"{id}\")]` | `@GetMapping(\"/{id}\")` |\n| `[FromBody]` | `@RequestBody` |\n| `[FromRoute]` | `@PathVariable` |\n| `[FromQuery]` | `@RequestParam` |\n\n## Test de Feynman\n\n« Un contrôleur regroupe des actions. Le routing par attributs (`[Route]`, `[HttpGet(\"{id}\")]`) associe une URL+verbe à une action. Les attributs `[From...]` disent où lire chaque paramètre : `[FromRoute]` dans l'URL, `[FromQuery]` dans la query string, `[FromBody]` dans le JSON du corps. `[ApiController]` ajoute la validation automatique et les bonnes réponses d'erreur. »\n\n## À retenir\n\n- Contrôleur Web API = classe `[ApiController] : ControllerBase`.\n- Routing par attributs : `[Route]` sur la classe, `[HttpXxx]` sur l'action.\n- `[FromRoute]`, `[FromQuery]`, `[FromBody]` indiquent la source du paramètre.\n- `CreatedAtAction` renvoie 201 avec un en-tête `Location`.",
      "playground": null,
      "quiz": [
        {
          "question": "Quel attribut indique qu'un paramètre doit être lu dans le corps JSON de la requête ?",
          "options": ["[FromRoute]", "[FromQuery]", "[FromBody]", "[FromHeader]"],
          "correctIndex": 2,
          "explanation": "[FromBody] lie le paramètre au corps de la requête (généralement du JSON désérialisé). [FromRoute] lit un segment d'URL, [FromQuery] la query string, [FromHeader] un en-tête."
        },
        {
          "question": "Que fait l'attribut [ApiController] sur un contrôleur ?",
          "options": ["Rien, c'est purement décoratif", "Il active la validation automatique du modèle, l'inférence du binding et les réponses ProblemDetails", "Il transforme le contrôleur en page Razor", "Il désactive le routing par attributs"],
          "correctIndex": 1,
          "explanation": "[ApiController] active des comportements adaptés aux API : validation automatique (400 si modèle invalide), inférence des sources de binding et réponses d'erreur au format ProblemDetails."
        },
        {
          "question": "Avec [Route(\"api/[controller]\")], à quelle URL répond CommandesController ?",
          "options": ["api/CommandesController", "api/commandes", "controller/commandes", "api/[controller]"],
          "correctIndex": 1,
          "explanation": "Le token [controller] est remplacé par le nom du contrôleur sans le suffixe 'Controller', en minuscules : CommandesController devient api/commandes."
        }
      ]
    },
    {
      "id": "binding-validation-http",
      "title": "5. Model binding, validation et codes HTTP REST",
      "markdown": "## La métaphore : le formulaire de douane\n\nÀ la douane, tu remplis un **formulaire**. L'agent (1) **transcrit** ce que tu écris dans des cases officielles (le *model binding*), puis (2) **vérifie** que tout est cohérent : nom non vide, âge plausible (la *validation*). Si une case est fausse, il te **renvoie le formulaire** avec les erreurs (un code 400). Si tout est bon, il te **laisse passer** (un code 200/201).\n\n## Le model binding\n\nLe **model binding** est le processus qui convertit les données brutes de la requête HTTP (segments d'URL, query string, JSON du corps, en-têtes) en **objets C#** passés en paramètres de ton action. C'est lui qui transforme `{ \"client\": \"Acme\", \"montant\": 150 }` en un objet `CreerCommandeDto` rempli.\n\n```csharp\npublic class CreerCommandeDto\n{\n    public string Client { get; set; } = string.Empty;\n    public decimal Montant { get; set; }\n}\n\n[HttpPost]\npublic IActionResult Creer([FromBody] CreerCommandeDto dto) { /* dto est déjà rempli */ }\n```\n\n## La validation par annotations (Data Annotations)\n\nOn déclare les règles directement sur le modèle avec des attributs du namespace `System.ComponentModel.DataAnnotations`.\n\n```csharp\nusing System.ComponentModel.DataAnnotations;\n\npublic class CreerCommandeDto\n{\n    [Required(ErrorMessage = \"Le client est obligatoire.\")]\n    [StringLength(100, MinimumLength = 2)]\n    public string Client { get; set; } = string.Empty;\n\n    [Range(0.01, 1_000_000)]\n    public decimal Montant { get; set; }\n\n    [EmailAddress]\n    public string? EmailContact { get; set; }\n}\n```\n\n| Attribut | Vérifie |\n|---|---|\n| `[Required]` | Valeur présente (non nulle) |\n| `[StringLength]` / `[MinLength]` / `[MaxLength]` | Longueur d'une chaîne |\n| `[Range]` | Valeur numérique dans un intervalle |\n| `[EmailAddress]` | Format e-mail |\n| `[RegularExpression]` | Correspond à un motif |\n\n### Validation automatique avec `[ApiController]`\n\nGrâce à `[ApiController]`, si le modèle est invalide, ASP.NET Core **renvoie automatiquement un 400 Bad Request** avec le détail des erreurs, **avant** même d'entrer dans le corps de ton action. Tu n'as pas besoin d'écrire `if (!ModelState.IsValid)`.\n\nSans `[ApiController]`, tu vérifierais manuellement :\n\n```csharp\nif (!ModelState.IsValid)\n    return BadRequest(ModelState);\n```\n\n## Les codes HTTP : parler le langage REST\n\nUne bonne API renvoie le **bon code de statut**. C'est essentiel et souvent testé en entretien.\n\n| Code | Nom | Quand l'utiliser |\n|---|---|---|\n| **200** | OK | Succès d'un GET/PUT, réponse avec contenu |\n| **201** | Created | Ressource créée (POST), avec en-tête `Location` |\n| **204** | No Content | Succès sans contenu (ex. DELETE, PUT) |\n| **400** | Bad Request | Données invalides (validation) |\n| **401** | Unauthorized | Non **authentifié** (identité inconnue) |\n| **403** | Forbidden | Authentifié mais **pas le droit** |\n| **404** | Not Found | Ressource inexistante |\n| **409** | Conflict | Conflit (ex. doublon, version concurrente) |\n| **500** | Internal Server Error | Erreur côté serveur non gérée |\n\n> Piège fréquent : **401 vs 403**. 401 = « je ne sais pas qui tu es » (pas/mauvais token). 403 = « je sais qui tu es, mais tu n'as pas le droit ».\n\n## Construire la réponse : IActionResult et ActionResult<T>\n\nTes actions renvoient en général un de ces types :\n\n- **`IActionResult`** : tu contrôles le code de statut via des méthodes d'aide (`Ok()`, `NotFound()`, etc.), sans type de contenu fixé.\n- **`ActionResult<T>`** : combine un type de retour fort `T` (utile pour la doc/Swagger) ET la possibilité de renvoyer des résultats de statut.\n\n```csharp\n[HttpGet(\"{id}\")]\npublic ActionResult<Commande> GetUne(int id)\n{\n    var c = _service.Trouver(id);\n    if (c is null) return NotFound();   // 404\n    return Ok(c);                       // 200 + JSON\n}\n```\n\nLes **méthodes d'aide** de `ControllerBase` produisent le bon code :\n\n| Méthode | Code |\n|---|---|\n| `Ok(obj)` | 200 |\n| `CreatedAtAction(...)` | 201 |\n| `NoContent()` | 204 |\n| `BadRequest(...)` | 400 |\n| `Unauthorized()` | 401 |\n| `Forbid()` | 403 |\n| `NotFound()` | 404 |\n| `Conflict(...)` | 409 |\n\n### Parallèle Spring Boot : ResponseEntity\n\nL'équivalent de `ActionResult<T>` côté Java/Spring Boot est **`ResponseEntity<T>`** :\n\n```java\n// Spring Boot\n@GetMapping(\"/{id}\")\npublic ResponseEntity<Commande> getUne(@PathVariable int id) {\n    Commande c = service.trouver(id);\n    if (c == null) return ResponseEntity.notFound().build(); // 404\n    return ResponseEntity.ok(c);                              // 200\n}\n```\n\n`ResponseEntity.ok(c)` ≈ `Ok(c)`, `ResponseEntity.notFound()` ≈ `NotFound()`, `ResponseEntity.status(HttpStatus.CREATED)` ≈ `Created/CreatedAtAction`.\n\n## ProblemDetails : un format d'erreur standard\n\n**ProblemDetails** (RFC 7807) est le format JSON standard pour décrire une erreur d'API. ASP.NET Core l'utilise automatiquement (avec `[ApiController]`) pour les 400, 404, 500, etc.\n\n```json\n{\n  \"type\": \"https://tools.ietf.org/html/rfc7231#section-6.5.1\",\n  \"title\": \"One or more validation errors occurred.\",\n  \"status\": 400,\n  \"errors\": {\n    \"Client\": [\"Le client est obligatoire.\"]\n  }\n}\n```\n\nTu peux aussi le renvoyer toi-même :\n\n```csharp\nreturn Problem(\n    title: \"Commande introuvable\",\n    detail: $\"Aucune commande avec l'id {id}\",\n    statusCode: StatusCodes.Status404NotFound);\n```\n\nAvantage : un format **uniforme** que tous les clients peuvent parser de la même manière.\n\n## Test de Feynman\n\n« Le **model binding** transforme la requête HTTP en objets C#. La **validation** (Data Annotations + `[ApiController]`) renvoie un 400 automatique si le modèle est invalide. Je renvoie le **bon code HTTP** : 200/201/204 en succès, 400/401/403/404/409 en erreur client, 500 en erreur serveur. J'utilise `ActionResult<T>` (équivalent `ResponseEntity<T>` en Spring) et les erreurs suivent le format **ProblemDetails**. »\n\n## À retenir\n\n- Model binding = requête HTTP → objets C#.\n- Validation par Data Annotations ; `[ApiController]` ⇒ 400 automatique.\n- 401 = non authentifié ; 403 = authentifié sans droit.\n- `ActionResult<T>` + méthodes d'aide (`Ok`, `NotFound`, `CreatedAtAction`).\n- Erreurs au format ProblemDetails (RFC 7807).",
      "playground": null,
      "quiz": [
        {
          "question": "Quelle est la différence entre les codes HTTP 401 et 403 ?",
          "options": ["401 = ressource introuvable ; 403 = erreur serveur", "401 = non authentifié (identité inconnue) ; 403 = authentifié mais sans droit", "Ils sont identiques", "401 = succès ; 403 = redirection"],
          "correctIndex": 1,
          "explanation": "401 Unauthorized signifie que l'utilisateur n'est pas authentifié (token absent/invalide). 403 Forbidden signifie qu'il est authentifié mais n'a pas la permission d'accéder à la ressource."
        },
        {
          "question": "Avec [ApiController], que se passe-t-il si le modèle envoyé échoue à la validation par Data Annotations ?",
          "options": ["L'action s'exécute quand même avec des valeurs par défaut", "Une réponse 400 Bad Request est renvoyée automatiquement avant l'exécution de l'action", "Une exception 500 est levée systématiquement", "Le framework ignore les erreurs"],
          "correctIndex": 1,
          "explanation": "[ApiController] active la validation automatique : si ModelState est invalide, un 400 Bad Request avec le détail des erreurs (ProblemDetails) est renvoyé sans entrer dans le corps de l'action."
        },
        {
          "question": "Quel est l'équivalent ASP.NET Core de ResponseEntity<T> de Spring Boot ?",
          "options": ["HttpContext", "ActionResult<T>", "IServiceCollection", "DbContext"],
          "correctIndex": 1,
          "explanation": "ActionResult<T> permet de renvoyer soit une valeur typée T, soit un résultat de statut (NotFound, BadRequest...), exactement comme ResponseEntity<T> côté Spring Boot."
        }
      ]
    },
    {
      "id": "bonnes-pratiques",
      "title": "6. Bonnes pratiques (DTO, async, configuration)",
      "markdown": "## La métaphore : le menu du restaurant\n\nLe **menu** d'un restaurant ne te montre pas la cuisine, les fournisseurs ni les stocks : il te présente juste ce que tu peux commander, dans un format clair. Tes **entités** internes (table en base, données sensibles) sont la cuisine ; tes **DTO** sont le menu exposé au client. On ne sert jamais la cuisine brute au client.\n\n## DTO vs Entité : ne jamais exposer ses entités\n\nUn **DTO** (*Data Transfer Object*) est un objet simple dédié à l'échange de données avec l'extérieur. Une **entité** est l'objet métier mappé à la base de données (souvent via Entity Framework Core).\n\n**Pourquoi séparer les deux ?**\n\n- **Sécurité** : éviter d'exposer des champs sensibles (`MotDePasseHash`, `EstAdmin`).\n- **Over-posting** : empêcher un client de modifier des champs qu'il ne devrait pas (ex. envoyer `EstAdmin: true` dans le corps).\n- **Découplage** : ton API publique ne change pas à chaque modification du schéma de base.\n- **Clarté** : le DTO ne contient que ce dont le client a besoin.\n\n```csharp\n// Entité (base de données) — NE PAS exposer directement\npublic class Utilisateur\n{\n    public int Id { get; set; }\n    public string Email { get; set; } = string.Empty;\n    public string MotDePasseHash { get; set; } = string.Empty; // sensible !\n    public bool EstAdmin { get; set; }                          // sensible !\n}\n\n// DTO de sortie — ce que le client voit\npublic record UtilisateurDto(int Id, string Email);\n\n// DTO d'entrée — ce que le client a le droit d'envoyer\npublic record CreerUtilisateurDto(string Email, string MotDePasse);\n\n// Mapping (à la main ou via une lib comme AutoMapper / Mapster)\nvar dto = new UtilisateurDto(u.Id, u.Email);\n```\n\n> En entretien : « Pourquoi ne pas renvoyer directement ton entité EF Core ? » Réponse : fuite de données sensibles, risque d'over-posting, et couplage fort entre l'API et la base.\n\n## async / await : ne pas bloquer les threads\n\nUne Web API gère **beaucoup de requêtes simultanées**. Les opérations d'**entrées/sorties** (base de données, appels réseau, fichiers) sont lentes. Si tu les exécutes de manière **bloquante**, le thread reste « coincé » à attendre et ne peut pas servir d'autres requêtes.\n\nAvec **`async`/`await`**, pendant l'attente d'une I/O, le thread est **libéré** pour traiter d'autres requêtes. Résultat : meilleure **scalabilité**.\n\n```csharp\n[HttpGet(\"{id}\")]\npublic async Task<ActionResult<CommandeDto>> GetUne(int id)\n{\n    var commande = await _service.TrouverAsync(id); // libère le thread pendant l'attente DB\n    if (commande is null) return NotFound();\n    return Ok(commande);\n}\n```\n\n### Règles d'or de l'async\n\n- Retourne **`Task`** ou **`Task<T>`** depuis tes actions et services I/O.\n- **N'utilise jamais** `.Result` ou `.Wait()` sur une `Task` : ça **bloque** le thread et peut provoquer des **deadlocks**. Toujours `await`.\n- Propage `async` de bout en bout (contrôleur → service → repository → EF Core `...Async()`).\n- Passe un **`CancellationToken`** pour pouvoir annuler une requête abandonnée par le client.\n\n```csharp\n// MAUVAIS : bloque le thread, risque de deadlock\nvar commande = _service.TrouverAsync(id).Result;\n\n// BON : libère le thread\nvar commande = await _service.TrouverAsync(id);\n```\n\n## La configuration : appsettings.json et IOptions\n\nNe code **jamais en dur** les paramètres (chaînes de connexion, URLs, clés). Mets-les dans la **configuration**. Le fichier principal est **`appsettings.json`**, complété par des fichiers par environnement et des variables d'environnement.\n\n```json\n{\n  \"ConnectionStrings\": {\n    \"Defaut\": \"Server=...;Database=Maintenance;...\"\n  },\n  \"EmailOptions\": {\n    \"Expediteur\": \"no-reply@industream.com\",\n    \"DelaiSecondes\": 30\n  }\n}\n```\n\n### La cascade de configuration\n\nASP.NET Core fusionne plusieurs sources, par **ordre de priorité croissant** (la dernière gagne) :\n\n1. `appsettings.json`\n2. `appsettings.{Environment}.json` (ex. `appsettings.Production.json`)\n3. Variables d'environnement\n4. *User secrets* (en développement) / arguments en ligne de commande\n\nAinsi, on garde les secrets hors du code source : en dev via *user secrets*, en prod via variables d'environnement ou un coffre (Azure Key Vault…).\n\n### Lire la configuration proprement avec le pattern Options\n\nPlutôt que d'aller chercher des valeurs à la main partout, on lie une section de config à une classe fortement typée et on l'injecte via `IOptions<T>`.\n\n```csharp\npublic class EmailOptions\n{\n    public string Expediteur { get; set; } = string.Empty;\n    public int DelaiSecondes { get; set; }\n}\n\n// Program.cs : liaison de la section \"EmailOptions\"\nbuilder.Services.Configure<EmailOptions>(\n    builder.Configuration.GetSection(\"EmailOptions\"));\n\n// Lecture de la chaîne de connexion\nvar cs = builder.Configuration.GetConnectionString(\"Defaut\");\n\n// Injection dans un service\npublic class EmailSender\n{\n    private readonly EmailOptions _options;\n    public EmailSender(IOptions<EmailOptions> options) => _options = options.Value;\n}\n```\n\n## Quelques bonnes pratiques transverses\n\n- **Garder les contrôleurs fins** (« thin controllers ») : la logique métier va dans des **services**, pas dans les actions.\n- **Pas de logique métier dans Program.cs** : juste de la configuration.\n- **Logger** correctement (via `ILogger<T>` injecté), sans logguer de données sensibles.\n- **Gérer les erreurs globalement** (middleware d'exception) plutôt que des try/catch partout.\n- **Versionner l'API** si elle évolue (ex. `api/v1/...`).\n\n## Test de Feynman\n\n« J'expose des **DTO**, jamais mes entités EF Core, pour la sécurité et le découplage. J'écris mon code I/O en **async/await** pour ne pas bloquer les threads et mieux scaler, et je n'utilise jamais `.Result`. Je sors toute valeur configurable dans **appsettings.json**, avec une cascade par environnement, et je la lis proprement via `IOptions<T>`. Mes contrôleurs restent fins, la logique vit dans les services. »\n\n## À retenir\n\n| Bonne pratique | Pourquoi |\n|---|---|\n| DTO ≠ Entité | Sécurité, anti over-posting, découplage |\n| async/await partout en I/O | Scalabilité, threads non bloqués |\n| Jamais `.Result` / `.Wait()` | Évite blocages et deadlocks |\n| Config dans appsettings + IOptions | Pas de valeurs en dur, par environnement |\n| Secrets hors du code | User secrets (dev), variables d'env (prod) |\n| Contrôleurs fins | Logique métier dans les services |",
      "playground": null,
      "quiz": [
        {
          "question": "Pourquoi exposer des DTO plutôt que directement les entités Entity Framework dans une Web API ?",
          "options": ["Parce que les entités ne se sérialisent pas en JSON", "Pour la sécurité (champs sensibles), éviter l'over-posting et découpler l'API de la base", "Parce que c'est obligatoire pour compiler", "Pour accélérer les requêtes SQL"],
          "correctIndex": 1,
          "explanation": "Les DTO évitent d'exposer des champs sensibles, empêchent l'over-posting (modification de champs interdits), et découplent le contrat de l'API du schéma de la base de données."
        },
        {
          "question": "Pourquoi éviter d'utiliser .Result ou .Wait() sur une Task dans une action ASP.NET Core ?",
          "options": ["Cela ne compile pas", "Cela bloque le thread et peut provoquer un deadlock ; il faut utiliser await", "Cela renvoie toujours null", "Cela double la latence de la base de données seulement"],
          "correctIndex": 1,
          "explanation": ".Result et .Wait() bloquent le thread appelant en attendant la Task, ce qui réduit la scalabilité et peut causer des deadlocks. On utilise await pour libérer le thread pendant l'attente."
        },
        {
          "question": "Quel mécanisme permet de lire une section d'appsettings.json sous forme de classe fortement typée injectable ?",
          "options": ["AddSingleton<string>", "Le pattern Options avec IOptions<T> et services.Configure<T>", "Console.ReadLine", "Les Data Annotations"],
          "correctIndex": 1,
          "explanation": "Le pattern Options lie une section de configuration à une classe via services.Configure<T>(...GetSection(...)) puis l'injecte avec IOptions<T>, évitant de lire la config à la main partout."
        }
      ]
    }
  ]
};
