/* ============================================================
   Questions additionnelles : Java, Spring Boot, Docker
   (ajoutées à la banque QUESTIONS définie dans questions.js)
   ============================================================ */

const JAVA_MCQ = [
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quel est le résultat de l'exécution de ce code concernant l'immutabilité des String ?","code":"String a = \"hello\";\nString b = a.concat(\" world\");\nSystem.out.println(a);","options":["hello","hello world","world","Une exception est levée"],"correctIndex":0,"explanation":"Les String sont immuables en Java. La méthode concat() ne modifie pas 'a' : elle retourne un nouvel objet String (assigné à 'b'). La variable 'a' référence toujours \"hello\".","difficulty":"easy"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la complexité de l'accès à un élément par son index (get(i)) sur une ArrayList comparée à une LinkedList ?","code":null,"options":["ArrayList O(1), LinkedList O(n)","ArrayList O(n), LinkedList O(1)","Les deux O(1)","Les deux O(n)"],"correctIndex":0,"explanation":"ArrayList s'appuie sur un tableau interne : l'accès par index est O(1). LinkedList est une liste doublement chaînée : atteindre l'index i nécessite de parcourir les noeuds, donc O(n). LinkedList n'est avantageuse que pour les insertions/suppressions en tête/queue.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Que se passe-t-il si on redéfinit equals() sans redéfinir hashCode() pour une classe utilisée comme clé dans un HashMap ?","code":null,"options":["Deux objets 'égaux' peuvent ne pas être retrouvés dans le HashMap","Une erreur de compilation","hashCode() est généré automatiquement de façon cohérente","Le HashMap ignore equals() et fonctionne normalement"],"correctIndex":0,"explanation":"Le contrat impose : si a.equals(b) alors a.hashCode() == b.hashCode(). Sans redéfinir hashCode(), deux objets logiquement égaux peuvent avoir des hash différents et tomber dans des buckets différents : on ne retrouve plus la clé. Il faut TOUJOURS redéfinir les deux ensemble.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Parmi ces exceptions, laquelle est une exception 'checked' (vérifiée à la compilation) ?","code":null,"options":["IOException","NullPointerException","ArrayIndexOutOfBoundsException","IllegalArgumentException"],"correctIndex":0,"explanation":"IOException hérite de Exception (mais pas de RuntimeException) : c'est une exception checked, le compilateur force à la gérer (try/catch ou throws). Les trois autres héritent de RuntimeException : ce sont des exceptions unchecked.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Que garantit l'instruction try-with-resources introduite en Java 7 ?","code":"try (BufferedReader br = new BufferedReader(new FileReader(\"f.txt\"))) {\n    return br.readLine();\n}","options":["La ressource est fermée automatiquement, même en cas d'exception","La ressource n'est jamais fermée","Il faut appeler close() manuellement dans un finally","La ressource est fermée uniquement si aucune exception n'est levée"],"correctIndex":0,"explanation":"Toute ressource déclarée dans le try-with-resources doit implémenter AutoCloseable. Sa méthode close() est appelée automatiquement à la fin du bloc, qu'une exception soit levée ou non, ce qui évite les fuites de ressources et le finally manuel.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quelle affirmation est correcte concernant une classe abstraite et une interface en Java 8+ ?","code":null,"options":["Une classe peut implémenter plusieurs interfaces mais hériter d'une seule classe (abstraite ou non)","Une interface ne peut contenir aucune méthode avec un corps","Une classe abstraite ne peut pas avoir de constructeur","On peut instancier une classe abstraite avec new"],"correctIndex":0,"explanation":"Java ne supporte pas l'héritage multiple de classes : une classe étend une seule classe (extends) mais peut implémenter plusieurs interfaces (implements). Depuis Java 8, une interface peut avoir des méthodes default/static avec un corps. Une classe abstraite peut avoir un constructeur mais ne peut pas être instanciée directement.","difficulty":"easy"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quel est le résultat de ce pipeline de Stream ?","code":"List<Integer> nums = List.of(1, 2, 3, 4, 5);\nint r = nums.stream()\n            .filter(n -> n % 2 == 0)\n            .mapToInt(n -> n * n)\n            .sum();\nSystem.out.println(r);","options":["20","30","55","9"],"correctIndex":0,"explanation":"filter garde les pairs (2 et 4), mapToInt les met au carré (4 et 16), sum additionne : 4 + 16 = 20. filter et map sont des opérations intermédiaires (lazy), sum est l'opération terminale qui déclenche le calcul.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la bonne façon d'utiliser Optional pour fournir une valeur par défaut quand il est vide ?","code":"Optional<String> name = Optional.empty();","options":["name.orElse(\"inconnu\")","name.get()","name.isPresent()","name.ifPresent(\"inconnu\")"],"correctIndex":0,"explanation":"orElse(valeur) retourne la valeur contenue si présente, sinon la valeur par défaut fournie. Appeler get() sur un Optional vide lève NoSuchElementException. isPresent() renvoie un booléen et ifPresent() exécute une action, ils ne fournissent pas de valeur de repli.","difficulty":"easy"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Que génère automatiquement un record déclaré ainsi : record Point(int x, int y) {} ?","code":"record Point(int x, int y) {}","options":["Un constructeur, les accesseurs x()/y(), equals(), hashCode() et toString()","Uniquement un constructeur","Des setters setX()/setY()","Rien, il faut tout écrire à la main"],"correctIndex":0,"explanation":"Un record (Java 16+) génère automatiquement : un constructeur canonique, des accesseurs nommés comme les composants (x(), y()), ainsi que equals(), hashCode() et toString(). Les champs sont final : un record est immuable, il n'y a donc pas de setters.","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la différence clé entre == et equals() pour comparer deux objets String ?","code":"String a = new String(\"hi\");\nString b = new String(\"hi\");","options":["== compare les références, equals() compare le contenu","== compare le contenu, equals() compare les références","Les deux comparent le contenu","Les deux comparent les références"],"correctIndex":0,"explanation":"== compare les références (les deux objets sont-ils le même en mémoire ?). Ici a et b sont créés avec new : références différentes, donc a == b vaut false. equals() compare le contenu des caractères, donc a.equals(b) vaut true. Avec des littéraux mis dans le String pool, == peut renvoyer true par hasard, d'où l'importance d'utiliser equals().","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Que signifie le mot-clé static appliqué à une méthode ou un champ ?","code":null,"options":["Le membre appartient à la classe, pas à une instance","Le membre ne peut pas être modifié","Le membre est privé","Le membre est recréé pour chaque objet"],"correctIndex":0,"explanation":"static signifie que le membre appartient à la classe elle-même : il est partagé par toutes les instances et accessible via le nom de la classe sans créer d'objet. À ne pas confondre avec final, qui empêche la réassignation/redéfinition.","difficulty":"easy"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quel piège l'autoboxing peut-il provoquer dans cette comparaison ?","code":"Integer a = 1000;\nInteger b = 1000;\nSystem.out.println(a == b);","options":["false, car == compare les références et 1000 est hors du cache Integer (-128 à 127)","true, car les valeurs sont égales","Erreur de compilation","true, car Integer est mis en cache pour toutes les valeurs"],"correctIndex":0,"explanation":"Java met en cache les Integer de -128 à 127. Pour 1000 (hors cache), l'autoboxing crée deux objets distincts : == compare les références et renvoie false. Pour comparer des wrappers, utilisez equals(). (Avec 100 au lieu de 1000, == renverrait true à cause du cache, ce qui rend le piège trompeur.)","difficulty":"hard"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la différence entre Comparable et Comparator ?","code":null,"options":["Comparable définit l'ordre naturel via compareTo() dans la classe ; Comparator définit un ordre externe via compare()","Comparable est pour les nombres, Comparator pour les String","Les deux sont identiques","Comparator se déclare dans la classe à trier, Comparable à l'extérieur"],"correctIndex":0,"explanation":"Comparable<T> s'implémente dans la classe elle-même (méthode compareTo) et définit son ordre naturel unique. Comparator<T> est externe (méthode compare) et permet de définir plusieurs ordres de tri différents sans modifier la classe, par exemple via Comparator.comparing(...).","difficulty":"medium"},
  {"category":"Java","subcategory":"Language knowledge","type":"mcq","question":"Dans quel cas le mot-clé var (Java 10+) est-il INVALIDE ?","code":null,"options":["Comme type d'un champ de classe ou d'un paramètre de méthode","Pour une variable locale initialisée : var list = new ArrayList<String>()","Dans une boucle for : for (var i = 0; i < 10; i++)","Dans un for-each : for (var item : items)"],"correctIndex":0,"explanation":"var (inférence de type) est réservé aux variables locales avec une initialisation explicite. Il ne peut pas être utilisé pour un champ d'instance, un paramètre de méthode, un type de retour, ni une variable locale non initialisée (le compilateur ne pourrait pas inférer le type).","difficulty":"hard"}
];

const JAVA_CODE = [
  {"category":"Java","subcategory":"Problem solving","type":"code","language":"java","prompt":"Écrire une méthode statique isPalindrome qui retourne true si la chaîne donnée est un palindrome (se lit pareil dans les deux sens), en ignorant la casse. On suppose la chaîne non nulle.","starterCode":"public class Palindrome {\n    public static boolean isPalindrome(String word) {\n        // TODO: ignorer la casse et comparer la chaine a son inverse\n        return false;\n    }\n}","solution":"public class Palindrome {\n    public static boolean isPalindrome(String word) {\n        String w = word.toLowerCase();\n        int left = 0;\n        int right = w.length() - 1;\n        while (left < right) {\n            if (w.charAt(left) != w.charAt(right)) {\n                return false;\n            }\n            left++;\n            right--;\n        }\n        return true;\n    }\n}","className":"Palindrome","methodName":"isPalindrome","tests":[{"input":"\"Level\"","expected":"true"},{"input":"\"hello\"","expected":"false"},{"input":"\"Anna\"","expected":"true"},{"input":"\"a\"","expected":"true"}],"explanation":"On normalise la casse avec toLowerCase(), puis on compare les caractères symétriques avec deux pointeurs (début et fin) qui se rapprochent. Approche O(n) sans allouer de chaîne inversée. La détection de casse (Level, Anna) est le piège classique de cet exercice TestDome.","difficulty":"easy"},
  {"category":"Java","subcategory":"Problem solving","type":"code","language":"java","prompt":"Écrire une méthode statique fizzBuzz qui, pour un entier n, retourne \"FizzBuzz\" si n est divisible par 3 et 5, \"Fizz\" s'il est divisible par 3, \"Buzz\" s'il est divisible par 5, sinon la représentation textuelle du nombre.","starterCode":"public class FizzBuzz {\n    public static String fizzBuzz(int n) {\n        // TODO: tester d'abord le cas divisible par 3 ET 5\n        return \"\";\n    }\n}","solution":"public class FizzBuzz {\n    public static String fizzBuzz(int n) {\n        if (n % 15 == 0) {\n            return \"FizzBuzz\";\n        }\n        if (n % 3 == 0) {\n            return \"Fizz\";\n        }\n        if (n % 5 == 0) {\n            return \"Buzz\";\n        }\n        return String.valueOf(n);\n    }\n}","className":"FizzBuzz","methodName":"fizzBuzz","tests":[{"input":"15","expected":"\"FizzBuzz\""},{"input":"9","expected":"\"Fizz\""},{"input":"10","expected":"\"Buzz\""},{"input":"7","expected":"\"7\""}],"explanation":"Le piège est l'ordre des conditions : il faut tester le cas divisible par 3 ET 5 (n % 15 == 0) EN PREMIER, sinon le \"Fizz\" ou \"Buzz\" l'intercepte avant. On retourne le nombre via String.valueOf pour les autres cas.","difficulty":"easy"},
  {"category":"Java","subcategory":"Problem solving","type":"code","language":"java","prompt":"Écrire une méthode statique areAnagrams qui retourne true si les deux chaînes sont des anagrammes l'une de l'autre (mêmes lettres, mêmes quantités), en ignorant la casse. On suppose les deux chaînes non nulles.","starterCode":"import java.util.Arrays;\n\npublic class Anagram {\n    public static boolean areAnagrams(String a, String b) {\n        // TODO: comparer les deux chaines une fois leurs lettres triees\n        return false;\n    }\n}","solution":"import java.util.Arrays;\n\npublic class Anagram {\n    public static boolean areAnagrams(String a, String b) {\n        if (a.length() != b.length()) {\n            return false;\n        }\n        char[] c1 = a.toLowerCase().toCharArray();\n        char[] c2 = b.toLowerCase().toCharArray();\n        Arrays.sort(c1);\n        Arrays.sort(c2);\n        return Arrays.equals(c1, c2);\n    }\n}","className":"Anagram","methodName":"areAnagrams","tests":[{"input":"\"listen\", \"silent\"","expected":"true"},{"input":"\"Triangle\", \"Integral\"","expected":"true"},{"input":"\"hello\", \"world\"","expected":"false"},{"input":"\"abc\", \"ab\"","expected":"false"}],"explanation":"On normalise la casse, on convertit en tableaux de caractères que l'on trie, puis on compare avec Arrays.equals. Si deux mots ont les mêmes lettres triées, ce sont des anagrammes. Le test rapide des longueurs évite un tri inutile. Complexité O(n log n).","difficulty":"medium"},
  {"category":"Java","subcategory":"Problem solving","type":"code","language":"java","prompt":"Écrire une méthode statique mostFrequentChar qui retourne le caractère le plus fréquent d'une chaîne non vide. En cas d'égalité, retourner celui qui apparaît en premier dans la chaîne.","starterCode":"import java.util.HashMap;\nimport java.util.Map;\n\npublic class CharFrequency {\n    public static char mostFrequentChar(String text) {\n        // TODO: compter chaque caractere puis retourner le plus frequent\n        return ' ';\n    }\n}","solution":"import java.util.HashMap;\nimport java.util.Map;\n\npublic class CharFrequency {\n    public static char mostFrequentChar(String text) {\n        Map<Character, Integer> counts = new HashMap<>();\n        char best = text.charAt(0);\n        int bestCount = 0;\n        for (char c : text.toCharArray()) {\n            int count = counts.getOrDefault(c, 0) + 1;\n            counts.put(c, count);\n            if (count > bestCount) {\n                bestCount = count;\n                best = c;\n            }\n        }\n        return best;\n    }\n}","className":"CharFrequency","methodName":"mostFrequentChar","tests":[{"input":"\"aabbbc\"","expected":"'b'"},{"input":"\"abcabc\"","expected":"'a'"},{"input":"\"x\"","expected":"'x'"},{"input":"\"aaabbbb\"","expected":"'b'"}],"explanation":"On parcourt la chaîne une fois en comptant chaque caractère dans une HashMap via getOrDefault. On suit au passage le meilleur candidat ; comme on ne met à jour qu'avec un count STRICTEMENT supérieur, le premier caractère atteignant le maximum (donc le premier rencontré en cas d'égalité) est conservé. Complexité O(n).","difficulty":"medium"},
  {"category":"Java","subcategory":"Problem solving","type":"code","language":"java","prompt":"Écrire une méthode statique secondLargest qui retourne le deuxième plus grand élément DISTINCT d'un tableau d'entiers contenant au moins deux valeurs distinctes.","starterCode":"public class ArrayUtils {\n    public static int secondLargest(int[] numbers) {\n        // TODO: trouver le plus grand puis le deuxieme plus grand distinct\n        return 0;\n    }\n}","solution":"public class ArrayUtils {\n    public static int secondLargest(int[] numbers) {\n        int first = Integer.MIN_VALUE;\n        int second = Integer.MIN_VALUE;\n        for (int n : numbers) {\n            if (n > first) {\n                second = first;\n                first = n;\n            } else if (n > second && n != first) {\n                second = n;\n            }\n        }\n        return second;\n    }\n}","className":"ArrayUtils","methodName":"secondLargest","tests":[{"input":"new int[]{3, 1, 4, 1, 5}","expected":"4"},{"input":"new int[]{10, 10, 9}","expected":"9"},{"input":"new int[]{2, 1}","expected":"1"},{"input":"new int[]{-1, -2, -3}","expected":"-2"}],"explanation":"On garde deux variables (le plus grand et le second) en un seul parcours O(n). Quand on trouve un nouveau maximum, l'ancien devient le second. La condition n != first gère les doublons (10, 10, 9 -> second = 9) pour bien retourner le deuxième élément DISTINCT.","difficulty":"medium"},
  {"category":"Java","subcategory":"Reliability","type":"code","language":"java","prompt":"Écrire une méthode statique safeLength qui retourne le nombre de caractères d'une chaîne, mais qui retourne 0 si la chaîne passée est null (au lieu de lever une NullPointerException). Gérer aussi la chaîne vide.","starterCode":"public class SafeString {\n    public static int safeLength(String text) {\n        // TODO: gerer le cas null sans lever d'exception\n        return text.length();\n    }\n}","solution":"public class SafeString {\n    public static int safeLength(String text) {\n        if (text == null) {\n            return 0;\n        }\n        return text.length();\n    }\n}","className":"SafeString","methodName":"safeLength","tests":[{"input":"\"hello\"","expected":"5"},{"input":"null","expected":"0"},{"input":"\"\"","expected":"0"},{"input":"\"a b\"","expected":"3"}],"explanation":"Test de fiabilité (reliability) typique : le code de départ appelle text.length() sans vérifier null, ce qui lève une NullPointerException. Il faut ajouter une garde 'if (text == null) return 0;' AVANT tout déréférencement. La chaîne vide n'est pas null et a bien une longueur de 0.","difficulty":"easy"}
];

const SPRINGBOOT_MCQ = [
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Qu'est-ce que l'inversion de contrôle (IoC) dans Spring ?","code":null,"options":["Un mécanisme qui inverse l'ordre d'exécution des méthodes d'une classe","Le fait que ce soit le conteneur Spring, et non la classe elle-même, qui crée et fournit les objets dont elle dépend","Une technique pour exécuter le code métier avant le code d'infrastructure","Un patron qui permet d'écrire des contrôleurs REST sans annotations"],"correctIndex":1,"explanation":"L'inversion de contrôle signifie que la responsabilité de créer et d'assembler les objets (beans) est confiée au conteneur Spring plutôt qu'au code applicatif. L'injection de dépendances est la mise en oeuvre concrète de l'IoC : le conteneur injecte les dépendances au lieu que la classe les instancie elle-même (réduisant le couplage).","difficulty":"easy"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Parmi ces annotations stéréotypes, laquelle indique spécifiquement une classe de la couche d'accès aux données et active la traduction des exceptions de persistance ?","code":null,"options":["@Service","@Component","@Repository","@Controller"],"correctIndex":2,"explanation":"@Repository est un stéréotype spécialisé de @Component pour la couche DAO/persistance. En plus d'être détecté par le component scanning, il active la traduction automatique des exceptions techniques (ex. SQLException) en DataAccessException de Spring. @Service marque la couche métier, @Controller la couche web ; @Component est le stéréotype générique.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Pourquoi l'injection par constructeur est-elle aujourd'hui préférée à l'injection par champ (@Autowired sur un attribut) ?","code":"@Service\npublic class OrderService {\n    private final PaymentClient paymentClient;\n\n    public OrderService(PaymentClient paymentClient) {\n        this.paymentClient = paymentClient;\n    }\n}","options":["Elle est plus rapide à l'exécution car Spring n'utilise pas la réflexion","Elle permet de déclarer les dépendances en final (immuabilité), garantit qu'elles sont présentes à la création de l'objet et facilite les tests unitaires","Elle est la seule façon d'injecter plusieurs beans du même type","Elle évite d'avoir à déclarer la classe comme bean Spring"],"correctIndex":1,"explanation":"L'injection par constructeur permet de rendre les dépendances final (immuables), garantit un objet toujours dans un état valide (dépendances obligatoires présentes dès la construction) et rend les tests faciles : on instancie la classe en passant des mocks, sans conteneur Spring. Depuis Spring 4.3, @Autowired est même optionnel si la classe n'a qu'un seul constructeur.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Quel est le scope par défaut d'un bean Spring ?","code":null,"options":["prototype : une nouvelle instance à chaque injection/demande","singleton : une seule instance partagée pour tout le conteneur","request : une instance par requête HTTP","Aucun, il faut toujours préciser @Scope"],"correctIndex":1,"explanation":"Par défaut un bean est en scope singleton : le conteneur crée une unique instance partagée dans tout l'ApplicationContext. Avec @Scope(\"prototype\"), Spring crée une nouvelle instance à chaque demande du bean. Attention : un singleton est partagé, il faut donc éviter d'y stocker un état mutable propre à une requête.","difficulty":"easy"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"À quoi équivaut l'annotation @SpringBootApplication ?","code":"@SpringBootApplication\npublic class App {\n    public static void main(String[] args) {\n        SpringApplication.run(App.class, args);\n    }\n}","options":["@Controller + @ResponseBody + @RequestMapping","@Configuration + @EnableAutoConfiguration + @ComponentScan","@Component + @Bean + @Profile","@Entity + @Repository + @Service"],"correctIndex":1,"explanation":"@SpringBootApplication est une méta-annotation de commodité qui combine @Configuration (classe de config), @EnableAutoConfiguration (active l'auto-configuration en fonction des dépendances du classpath) et @ComponentScan (scan des composants du package courant et de ses sous-packages).","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Comment l'auto-configuration de Spring Boot décide-t-elle de configurer (ou non) un composant comme une DataSource ?","code":null,"options":["Elle configure systématiquement tous les composants possibles, puis désactive les inutiles","Elle se base sur des conditions, notamment la présence de classes/dépendances sur le classpath et l'absence de bean déjà défini par l'utilisateur (@ConditionalOn...)","Elle lit uniquement application.properties et n'active rien sans propriété explicite","Elle demande à l'utilisateur au démarrage quels composants activer"],"correctIndex":1,"explanation":"L'auto-configuration repose sur des classes conditionnelles utilisant des annotations comme @ConditionalOnClass, @ConditionalOnMissingBean, @ConditionalOnProperty. Spring Boot regarde le classpath (ex. présence de H2/JDBC) et configure des beans par défaut, sauf si vous avez déjà défini votre propre bean : votre configuration explicite prime toujours.","difficulty":"hard"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Que fait l'annotation @Value dans l'exemple suivant ?","code":"@Component\npublic class MailService {\n    @Value(\"${app.mail.from}\")\n    private String fromAddress;\n}","options":["Elle injecte la valeur de la propriété app.mail.from (depuis application.properties/yml ou variable d'environnement)","Elle déclare une nouvelle propriété nommée fromAddress dans application.properties","Elle valide que fromAddress n'est pas null au démarrage","Elle crée un bean nommé app.mail.from"],"correctIndex":0,"explanation":"@Value avec la syntaxe ${...} injecte la valeur d'une propriété externalisée (application.properties/yml, variables d'environnement, arguments de ligne de commande). On peut fournir une valeur par défaut avec ${app.mail.from:noreply@demo.com}. Pour regrouper plusieurs propriétés liées, on préfère souvent @ConfigurationProperties.","difficulty":"easy"},
  {"category":"Spring Boot","subcategory":"Language knowledge","type":"mcq","question":"Concernant les starters, le serveur embarqué et les profils, quelle affirmation est CORRECTE ?","code":null,"options":["Un starter comme spring-boot-starter-web embarque par défaut un serveur Tomcat, et @Profile permet d'activer un bean uniquement pour certains profils (ex. dev, prod)","Les starters remplacent le besoin d'un serveur d'application : il faut toujours déployer un .war sur un Tomcat externe","@Profile sert à définir le scope d'un bean (singleton ou prototype)","application.yml et application.properties ne peuvent pas coexister et yml est obligatoire pour les profils"],"correctIndex":0,"explanation":"Les starters sont des dépendances pré-packagées : spring-boot-starter-web tire Spring MVC et un Tomcat embarqué, ce qui permet de lancer un .jar exécutable sans serveur externe. @Profile(\"dev\") active un bean uniquement quand le profil correspondant est actif (spring.profiles.active). properties et yml expriment la même chose : yml est plus lisible pour des structures hiérarchiques mais reste équivalent.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Quelle est la différence essentielle entre @Controller et @RestController ?","code":null,"options":["@Controller gère le HTTP, @RestController gère uniquement le WebSocket","@RestController combine @Controller et @ResponseBody : les valeurs retournées sont sérialisées directement dans le corps de la réponse (JSON), alors que @Controller retourne par défaut un nom de vue à résoudre","Il n'y a aucune différence, ce sont des alias","@RestController interdit l'utilisation de @GetMapping et @PostMapping"],"correctIndex":1,"explanation":"@RestController = @Controller + @ResponseBody. Avec @RestController, le retour de chaque méthode est automatiquement sérialisé (généralement en JSON) dans le corps de la réponse, ce qui en fait le choix standard pour une API REST. @Controller seul retourne par défaut un nom de vue (Thymeleaf, JSP) à résoudre, sauf si on annote la méthode avec @ResponseBody.","difficulty":"easy"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Dans ce contrôleur, comment Spring lie-t-il les paramètres id et active ?","code":"@GetMapping(\"/users/{id}\")\npublic User get(@PathVariable Long id,\n                @RequestParam(defaultValue = \"true\") boolean active) {\n    ...\n}","options":["id vient du corps JSON et active de l'en-tête HTTP","id est extrait du chemin de l'URL (/users/42) et active vient de la query string (?active=false)","id et active viennent tous deux de la query string","id vient de la query string et active du chemin de l'URL"],"correctIndex":1,"explanation":"@PathVariable lie une portion du chemin de l'URI (/users/{id} -> 42), typiquement pour identifier une ressource obligatoire. @RequestParam lie un paramètre de la query string (?active=false), idéal pour des critères optionnels de filtrage/tri/pagination ; defaultValue rend le paramètre facultatif. @RequestBody, lui, lie le corps de la requête (JSON) à un objet.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Quel intérêt apporte ResponseEntity par rapport à un simple retour d'objet dans une méthode de contrôleur REST ?","code":"return ResponseEntity.status(HttpStatus.CREATED)\n        .header(\"Location\", \"/users/\" + saved.getId())\n        .body(saved);","options":["Il permet de contrôler explicitement le code de statut HTTP, les en-têtes ET le corps de la réponse","Il convertit automatiquement l'entité en DTO","Il rend la méthode transactionnelle","Il est obligatoire dès qu'on utilise @RestController"],"correctIndex":0,"explanation":"ResponseEntity<T> représente toute la réponse HTTP : statut (ex. 201 Created, 404 Not Found), en-têtes (ex. Location) et corps. Cela donne un contrôle fin, utile par exemple pour renvoyer 201 à la création ou 404 si la ressource est absente. Sans ResponseEntity, un retour d'objet donne par défaut 200 OK.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Avec Spring Data JPA, que fait la méthode suivante déclarée dans un JpaRepository ?","code":"public interface UserRepository extends JpaRepository<User, Long> {\n    List<User> findByLastNameAndActiveTrue(String lastName);\n}","options":["Rien : il faut obligatoirement écrire une requête JPQL avec @Query","Spring génère automatiquement la requête à partir du nom de la méthode (utilisateurs dont lastName correspond et active = true)","Elle supprime tous les utilisateurs portant ce nom","Elle ne compile pas car une interface ne peut pas contenir de logique"],"correctIndex":1,"explanation":"C'est une méthode dérivée (derived query) : Spring Data analyse le nom (findBy + LastName + And + ActiveTrue) et génère automatiquement la requête correspondante au démarrage, sans écrire de SQL/JPQL. JpaRepository<User, Long> fournit déjà save, findById, findAll, delete... Pour des cas complexes, on bascule sur @Query.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Quel rôle jouent @Entity et @Id dans cette classe ?","code":"@Entity\npublic class Product {\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n    private String name;\n}","options":["@Entity expose la classe comme endpoint REST et @Id définit l'URL","@Entity marque la classe comme entité persistante (mappée à une table) et @Id désigne la clé primaire","@Entity crée un bean singleton et @Id son nom","Ce sont des annotations Spring Web, sans lien avec la base de données"],"correctIndex":1,"explanation":"Ce sont des annotations JPA. @Entity indique que la classe est une entité persistante mappée à une table. @Id désigne l'attribut clé primaire, et @GeneratedValue(strategy = IDENTITY) délègue la génération de l'id à la base (colonne auto-incrémentée). Ces annotations relèvent de la persistance, indépendamment de la couche web.","difficulty":"easy"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Comment déclencher la validation du corps reçu et où l'annotation de contrainte doit-elle se trouver ?","code":"public class CreateUserRequest {\n    @NotBlank\n    private String name;\n    @Email\n    private String email;\n}\n\n@PostMapping(\"/users\")\npublic ResponseEntity<User> create(@RequestBody CreateUserRequest req) { ... }","options":["Il faut ajouter @Valid devant @RequestBody (create(@Valid @RequestBody ...)) ; les contraintes (@NotBlank, @Email) se placent sur les champs du DTO","Aucune action : Spring valide automatiquement tout @RequestBody","Il faut mettre @Valid sur la classe CreateUserRequest, pas dans le contrôleur","Les contraintes doivent être placées dans le contrôleur, pas dans le DTO"],"correctIndex":0,"explanation":"Les contraintes Bean Validation (@NotNull, @NotBlank, @Email, @Size...) se placent sur les champs du DTO. La validation ne se déclenche que si on annote le paramètre avec @Valid : create(@Valid @RequestBody CreateUserRequest req). En cas d'échec, Spring lève une MethodArgumentNotValidException, qu'on traite généralement pour renvoyer un 400 Bad Request.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Quelle est la bonne approche pour centraliser la gestion des erreurs d'une API REST et renvoyer des réponses cohérentes ?","code":"@RestControllerAdvice\npublic class ApiExceptionHandler {\n    @ExceptionHandler(EntityNotFoundException.class)\n    public ResponseEntity<ApiError> handle(EntityNotFoundException ex) {\n        return ResponseEntity.status(HttpStatus.NOT_FOUND)\n                .body(new ApiError(ex.getMessage()));\n    }\n}","options":["Entourer chaque méthode de contrôleur d'un try/catch dupliqué","Utiliser @ControllerAdvice/@RestControllerAdvice avec des méthodes @ExceptionHandler pour gérer globalement les exceptions et choisir le code HTTP","Laisser remonter toutes les exceptions sans les traiter, Spring renvoie toujours le bon code","Mettre les @ExceptionHandler dans la classe @Entity"],"correctIndex":1,"explanation":"@ControllerAdvice (ou @RestControllerAdvice, qui ajoute @ResponseBody) regroupe la gestion d'erreurs de façon transversale. Les méthodes @ExceptionHandler interceptent un type d'exception et renvoient une ResponseEntity avec le bon statut (ex. 404) et un corps d'erreur structuré, évitant la duplication de try/catch dans chaque contrôleur.","difficulty":"medium"},
  {"category":"Spring Boot","subcategory":"Design","type":"mcq","question":"Pourquoi exposer un DTO plutôt que directement l'entité JPA dans une API REST, et que fait @Transactional sur une méthode de service ?","code":"@Transactional\npublic OrderDto place(CreateOrderRequest req) { ... }","options":["Le DTO évite d'exposer/coupler le modèle de base de données à l'API (champs sensibles, relations lazy, sérialisation), et @Transactional englobe la méthode dans une transaction (commit si succès, rollback si exception runtime)","Le DTO est plus rapide à instancier et @Transactional rend la méthode asynchrone","Il n'y a aucune raison d'utiliser des DTO, exposer l'entité est la bonne pratique","@Transactional crée un nouveau bean prototype à chaque appel"],"correctIndex":0,"explanation":"Exposer un DTO découple le contrat de l'API du schéma de base : on évite de divulguer des champs sensibles, on maîtrise la forme JSON et on évite les pièges des relations lazy/proxies Hibernate et des cycles de sérialisation. @Transactional ouvre une transaction autour de la méthode : commit si tout réussit, rollback automatique en cas d'exception non vérifiée (RuntimeException) par défaut ; readOnly=true optimise les lectures.","difficulty":"hard"}
];

const DOCKER_MCQ = [
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la différence fondamentale entre une image Docker et un conteneur ?","code":null,"options":["Une image est un processus en cours d'exécution, un conteneur est un fichier sur le disque","L'image est un modèle en lecture seule (le plan), le conteneur est une instance en cours d'exécution de cette image","Il n'y a aucune différence, ce sont deux mots pour la même chose","Une image tourne sur le kernel hôte, un conteneur tourne dans une machine virtuelle"],"correctIndex":1,"explanation":"L'image est un template immuable (en lecture seule) qui contient le code et ses dépendances ; c'est le 'plan'. Le conteneur est une instance vivante créée à partir de cette image, avec une couche d'écriture par-dessus. Une même image peut produire plusieurs conteneurs.","difficulty":"easy"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la principale différence entre un conteneur et une machine virtuelle (VM) ?","code":null,"options":["Le conteneur partage le kernel de l'OS hôte, la VM embarque son propre OS complet avec un hyperviseur","Le conteneur est toujours plus lent à démarrer que la VM","La VM partage le kernel de l'hôte, le conteneur a son propre kernel isolé","Il n'y a aucune isolation dans un conteneur, contrairement à une VM"],"correctIndex":0,"explanation":"Un conteneur partage le kernel de l'OS hôte et isole seulement les processus (via namespaces/cgroups), ce qui le rend léger et rapide à démarrer. Une VM virtualise le matériel via un hyperviseur et embarque un OS invité complet, donc plus lourde et plus lente, mais avec une isolation plus forte.","difficulty":"easy"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Dans un Dockerfile, quelle est la différence entre CMD et ENTRYPOINT ?","code":"FROM eclipse-temurin:21-jre\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]\nCMD [\"--spring.profiles.active=prod\"]","options":["CMD et ENTRYPOINT sont strictement identiques","ENTRYPOINT définit l'exécutable principal (difficile à écraser), CMD fournit des arguments/commande par défaut facilement écrasables au 'docker run'","CMD s'exécute au build, ENTRYPOINT s'exécute au démarrage du conteneur","ENTRYPOINT ne peut être utilisé qu'une seule fois par image, CMD autant de fois qu'on veut et toutes s'exécutent"],"correctIndex":1,"explanation":"ENTRYPOINT fixe l'exécutable de base du conteneur. CMD fournit des arguments par défaut (ou une commande par défaut s'il n'y a pas d'ENTRYPOINT). Les arguments passés à 'docker run' remplacent CMD, mais s'ajoutent à ENTRYPOINT. Si les deux sont présents en forme exec, CMD devient les arguments d'ENTRYPOINT.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Quelle est la différence entre les instructions COPY et ADD dans un Dockerfile ?","code":null,"options":["COPY peut télécharger des URLs, ADD ne fait que copier des fichiers locaux","ADD et COPY sont identiques, ADD est juste l'ancien nom","COPY copie simplement des fichiers/dossiers locaux ; ADD fait pareil mais peut aussi décompresser des archives tar locales et récupérer des URLs distantes","COPY ne fonctionne qu'avec des fichiers de moins de 1 Mo"],"correctIndex":2,"explanation":"COPY est explicite et simple : il copie des fichiers/dossiers du contexte de build vers l'image. ADD ajoute deux comportements 'magiques' : extraction automatique des archives tar locales et téléchargement d'URLs distantes. La bonne pratique recommande COPY par défaut (plus prévisible) et de réserver ADD aux cas où on a réellement besoin de ces fonctionnalités.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Que fait réellement l'instruction EXPOSE dans un Dockerfile ?","code":"EXPOSE 8080","options":["Elle publie le port et le rend accessible depuis l'extérieur de l'hôte automatiquement","Elle documente le port que le conteneur écoute, sans le publier ; il faut quand même -p au 'docker run' pour y accéder depuis l'hôte","Elle ouvre le port dans le pare-feu de l'OS hôte","Elle redirige automatiquement le port 8080 vers le port 80"],"correctIndex":1,"explanation":"EXPOSE est purement documentaire : elle indique quel port le conteneur écoute, mais ne publie rien. Pour rendre le port accessible depuis l'hôte, il faut un mapping explicite avec -p (ex: 'docker run -p 8080:8080') ou l'option -P qui publie tous les ports EXPOSE sur des ports aléatoires de l'hôte.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Que signifie la commande suivante ?","code":"docker run -p 8080:80 nginx","options":["Map le port 80 de l'hôte vers le port 8080 du conteneur","Map le port 8080 de l'hôte vers le port 80 du conteneur (format -p HÔTE:CONTENEUR)","Expose les ports 80 à 8080 simultanément","Lance nginx uniquement sur le port 8080 sans aucun lien avec l'hôte"],"correctIndex":1,"explanation":"Le format est -p <port_hôte>:<port_conteneur>. Ici, un accès à http://localhost:8080 sur l'hôte est redirigé vers le port 80 du conteneur où écoute nginx. Inverser l'ordre est une erreur classique en entretien.","difficulty":"easy"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Pourquoi dit-on qu'un conteneur est 'éphémère' (stateless) et comment persister des données ?","code":null,"options":["Un conteneur garde toutes ses données pour toujours ; les volumes servent juste à accélérer","Les données écrites dans la couche d'écriture du conteneur sont perdues quand il est supprimé ; il faut un volume (ou bind mount) pour persister les données hors du cycle de vie du conteneur","Les données survivent à 'docker rm' mais pas à 'docker stop'","Il est impossible de persister des données avec Docker, il faut une base externe obligatoirement"],"correctIndex":1,"explanation":"La couche d'écriture d'un conteneur est jetable : à sa suppression, ses données disparaissent. C'est le principe éphémère/stateless qui rend les conteneurs remplaçables et scalables. Pour conserver des données (ex: une base PostgreSQL), on monte un volume géré par Docker (ou un bind mount) qui survit à la suppression du conteneur.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Comment monter un volume nommé pour persister les données d'une base PostgreSQL ?","code":"docker run -d -v pgdata:/var/lib/postgresql/data postgres:16","options":["Le volume 'pgdata' est créé/géré par Docker et monté dans /var/lib/postgresql/data ; les données survivent à la suppression du conteneur","Cette commande copie /var/lib/postgresql/data dans un fichier nommé pgdata sur l'hôte au format texte","Le volume est détruit automatiquement à chaque 'docker stop'","-v ne sert qu'à exposer des ports, pas à monter des volumes"],"correctIndex":0,"explanation":"Avec -v <nom>:<chemin>, Docker crée et gère un volume nommé monté dans le conteneur. Le contenu écrit par PostgreSQL dans /var/lib/postgresql/data est stocké dans ce volume, qui persiste indépendamment du cycle de vie du conteneur (survit à stop et rm). On peut le réutiliser avec un nouveau conteneur.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Pour réduire le temps de rebuild, dans quel ordre placer ces instructions dans un Dockerfile Maven/Spring Boot ?","code":"COPY pom.xml .\nRUN mvn dependency:go-offline\nCOPY src ./src\nRUN mvn package","options":["L'ordre n'a aucun impact, le cache de build n'existe pas","Il faut copier tout le code d'abord, puis le pom.xml, pour aller plus vite","Copier pom.xml et télécharger les dépendances AVANT de copier src ; ainsi un simple changement de code source n'invalide pas la couche de dépendances en cache","Il faut tout mettre dans un seul RUN pour éviter tout cache"],"correctIndex":2,"explanation":"Chaque instruction crée une couche (layer) mise en cache. Dès qu'une couche change, toutes les suivantes sont réinvalidées. En copiant le pom.xml et en téléchargeant les dépendances avant le code source, on profite du cache : tant que les dépendances ne changent pas, modifier le code ne re-télécharge pas tout. C'est un piège classique en entretien.","difficulty":"hard"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Quel est l'intérêt d'un multi-stage build (build multi-étapes) ?","code":"FROM maven:3.9-eclipse-temurin-21 AS build\nWORKDIR /app\nCOPY . .\nRUN mvn package -DskipTests\n\nFROM eclipse-temurin:21-jre\nCOPY --from=build /app/target/app.jar app.jar\nENTRYPOINT [\"java\",\"-jar\",\"app.jar\"]","options":["Construire plusieurs images différentes en parallèle pour gagner du temps","Compiler dans une étape avec tous les outils (Maven, JDK), puis copier seulement l'artefact (le .jar) dans une image finale légère sans les outils de build","Lancer plusieurs conteneurs depuis un seul Dockerfile","Chiffrer le code source pour le protéger"],"correctIndex":1,"explanation":"Le multi-stage build sépare la phase de compilation (avec Maven + JDK complet, lourds) de l'image finale d'exécution. On copie uniquement le .jar produit (COPY --from=build) dans une image runtime minimale (JRE seul). Résultat : une image finale beaucoup plus petite et plus sécurisée, sans les outils de build. C'est très valorisé chez un junior.","difficulty":"hard"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Dans un fichier docker-compose, que garantit (et ne garantit pas) 'depends_on' ?","code":"services:\n  app:\n    build: .\n    depends_on:\n      - db\n  db:\n    image: postgres:16","options":["Il garantit que la base de données est totalement prête à accepter des connexions avant de démarrer 'app'","Il contrôle seulement l'ORDRE de démarrage (db démarre avant app), mais n'attend PAS que db soit réellement 'prête' ; il faut un healthcheck (condition: service_healthy) pour ça","Il fusionne les deux services en un seul conteneur","Il n'a aucun effet sur l'ordre de démarrage"],"correctIndex":1,"explanation":"depends_on définit l'ordre de démarrage : 'db' est lancé avant 'app'. Mais par défaut il attend seulement que le conteneur soit démarré, pas que le service interne (PostgreSQL) soit prêt à accepter des connexions. Pour cela il faut un healthcheck sur db et 'depends_on: db: condition: service_healthy'. C'est un piège d'entretien fréquent.","difficulty":"hard"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"À quoi sert un fichier .dockerignore ?","code":null,"options":["À lister les commandes interdites dans le Dockerfile","À exclure des fichiers/dossiers (ex: target/, .git, node_modules) du contexte de build envoyé au démon Docker, ce qui accélère le build et allège l'image","À ignorer les erreurs pendant le 'docker build'","À masquer des variables d'environnement secrètes au runtime"],"correctIndex":1,"explanation":".dockerignore fonctionne comme .gitignore : il exclut des fichiers du contexte de build envoyé au démon. Cela évite d'envoyer des dossiers inutiles (target/, node_modules/, .git/), accélère le build, réduit la taille de l'image et empêche d'inclure par accident des fichiers sensibles via un COPY trop large.","difficulty":"medium"},
  {"category":"Docker","subcategory":"Language knowledge","type":"mcq","question":"Comment inspecter les logs d'un conteneur en cours d'exécution puis ouvrir un shell à l'intérieur pour déboguer ?","code":null,"options":["docker logs <conteneur> puis docker exec -it <conteneur> /bin/sh","docker build <conteneur> puis docker run <conteneur> bash","docker ps -logs puis docker open <conteneur>","docker inspect --shell <conteneur>"],"correctIndex":0,"explanation":"'docker logs <conteneur>' affiche la sortie standard/erreur du conteneur (essentiel pour diagnostiquer un crash). 'docker exec -it <conteneur> /bin/sh' (ou /bin/bash) ouvre un shell interactif DANS un conteneur déjà en cours d'exécution. Attention : 'docker run' crée un NOUVEAU conteneur, alors que 'docker exec' agit sur un conteneur existant.","difficulty":"medium"}
];

// Ajout à la banque principale (QUESTIONS est défini dans questions.js)
QUESTIONS.push.apply(QUESTIONS, JAVA_MCQ);
QUESTIONS.push.apply(QUESTIONS, JAVA_CODE);
QUESTIONS.push.apply(QUESTIONS, SPRINGBOOT_MCQ);
QUESTIONS.push.apply(QUESTIONS, DOCKER_MCQ);
